// const multer = require('multer');

// // Set storage engine
// const storage = multer.memoryStorage(); // Using memory storage for MinIO

// // Initialize upload
// const upload = multer({ storage: storage }).single('image'); // 'image' is the name of the input field in the form

// module.exports = upload;
